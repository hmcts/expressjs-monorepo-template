name: E2E Stage

on:
  workflow_call:
    inputs:
      preview-urls:
        required: true
        type: string
        description: "Base64-encoded JSON of preview URLs"

jobs:
  e2e-test:
    name: E2E Tests
    uses: ./.github/workflows/_job-e2e-test.yml
    with:
      preview-urls: ${{ inputs.preview-urls }}
    secrets: inherit
