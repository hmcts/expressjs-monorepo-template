name: Main

on:
  push:
    branches:
      - master

concurrency:
  group: main-${{ github.sha }}
  cancel-in-progress: true

jobs:
  build-stage:
    name: Build
    uses: ./.github/workflows/stage.build.yml
    secrets: inherit
